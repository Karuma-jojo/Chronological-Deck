# M13 independent-review resolution — repair checkpoint

Scope: `ARC511` on `codex/t22-pedagogical-rebuild`. The independent adversarial review examined the prior pushed head `c40a17517d6dccd64350ba1263636e581f37c080` and returned **REPAIRS REQUIRED**. This is the builder's response to those findings, not the independent reviewer's follow-up acceptance. M13 stays outside the shared learner registry; M14 remains untouched.

| Finding | Exact repair | Verification and disposition |
| --- | --- | --- |
| ADV-01: all 17 guided sections gave the answer immediately | Each `sessions[i].lesson` now contains an explicit learner action and scaffold without its solved result. The distinct `guidedFeedback` field holds the check. Optional shared UI controls require a nonempty attempt before displaying it. | Structural test rejects missing feedback and inline duplication; candidate-route browser test exercises all 17 controls. **Repaired, follow-up pending.** |
| ADV-02: fresh Main labels inflated | S04, S05, S07, S10 and S16 are classified as retrieval with reasons based on the actual reasoning skeleton. S17 Main was revised to require a scale-invariant diagnostic choice between differently scaled candidates after the basis/reachability report. No count-based prestige target remains. | New guard pins these five corrected labels and the S17 public choice. **Repaired, follow-up pending.** |
| ADV-03: S13 claimed zero-vector handling without observation | The S13 ownership claim now says “Compute Euclidean norms from self-dot products and justify their nonnegativity.” Public Main and scoring rows observe exactly that. The zero-angle boundary is independently assessed in S15 Transfer and S16 Transfer. | Exact claim→prompt→rubric links and a known-bad wording check pass locally. **Repaired, follow-up pending.** |
| ADV-04: S11 silently changed object type | Lesson, Main and Transfer now state a subspace of some real `R^N`; the replacement proof remains the same finite argument. The design gate's support theorem states the same-space coordinate-subspace hypotheses. | Scope assertions check both public tasks and lesson; no matrix/rank premise imported. **Repaired, follow-up pending.** |
| ADV-05: S17 promised a diagnostic decision but prompted the calculation | Main now gives a team report goal and two unequal-scale candidate directions. It asks the learner to choose a diagnostic invariant under positive rescaling, then rank and justify. Reference compares `2/√22` with `3/√11`; raw dots are `200` versus `3`, so ranking by raw dot fails. The worked example no longer completes that full geometric comparison. | Numeric comparison and exact prompt/rubric guards pass; separate review must judge whether the choice is sufficiently uncued. **Repaired, follow-up pending.** |
| ADV-06: handoff/browser closure stale | The prior `c40a175` head had successful [Actions run 36099514227](https://github.com/Karuma-jojo/Chronological-Deck/actions/runs/36099514227). The repaired implementation head `c56f88e34a4e97990825109c5a0db34390722df3` has successful [Actions run 36121162825](https://github.com/Karuma-jojo/Chronological-Deck/actions/runs/36121162825): syntax, structural regressions, Chromium installation and browser workflow all completed. The browser log confirms the test-only M13 UI route rendered all 17 lessons, staged checks and 34 task/reference/rubric surfaces. | **Implementation verification complete; independent pedagogical follow-up pending.** |

## Version and exposure treatment

The old candidate never entered the selectable learner registry, so there are no M13 learner attempts to migrate and no old fixed task answer exposures recorded by this route. The published review text did expose examples, but the repaired S17 Main has a different obligation and new comparison instance. All lesson content changes use instruction version `m13-arc511-instruction-v2`. Public assessment changes use new task IDs and `obligationVersion:2` for S11 Main/Transfer and S17 Main; the other 31 fixed obligations keep `@1` and version 1. All claim evidence pointers and public-prompt snapshots were updated. A future import containing old candidate IDs would require an explicit policy; none are silently mapped to the new IDs.

## Rechecked bounds and limitations

The repaired pack retains 17 sessions, 34 assessments and 49 claims. S11 applies only to finite lists in a coordinate subspace; S14's scalar proof device does not import M15 projection; S15/S16 keep the zero-direction exception. A full cross-session tuple scan after moving guided answers found shared isolated coordinates but no assessment instance solved in instruction or staged feedback. All 49 syntax checks and 39 structural/semantic workflow commands pass locally and in [run 36121162825](https://github.com/Karuma-jojo/Chronological-Deck/actions/runs/36121162825). Its browser job passed after Chromium installation; the test-only interception previews the actual learner UI without publishing M13. A separate reviewer must re-evaluate guided usability, S17 freshness, ownership semantics and whole-module pedagogy before acceptance or learner-route publication.

Current status: **repaired awaiting follow-up**. No main merge, deployment, T25 change, legacy progress migration or M14 authoring.
