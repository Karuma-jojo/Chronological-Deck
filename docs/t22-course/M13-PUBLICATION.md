# M13 publication and main-merge handoff

Module `ARC511` — Vectors, Span, Basis & Dot Products. Branch `codex/t22-pedagogical-rebuild`. The user reported the follow-up close and explicitly authorized publication and merge to `main` on 2026-09-25. The separate follow-up report itself was not supplied in this thread; this is a user-authorized closure, not a claim that the builder independently reviewed its own repair.

## Published scope

The shared learner route now registers M13 as module 13. It retains 17 sessions, 34 fixed tasks and 49 exact ownership links. M01–M12 remain in order, with their source packs unchanged. The M13 semantic ledger is accepted with local bridges for typed vector/displacement conventions (S01–S02), matrix-free finite replacement in a coordinate subspace (S11), and the nonzero Euclidean angle condition (S14–S15). M14 matrices and M15 projection are still outside this module.

The instruction contract is `m13-arc511-instruction-v2`. S11 Main/Transfer and S17 Main use `@2` and obligation version 2; the other 31 task obligations remain at version 1. This is the first M13 learner-route publication. There were no M13 learner attempts in the earlier twelve-module route to migrate. Pre-publication review examples are not silently treated as independent evidence. Opening a learning note during a page visit now keeps assistance provenance across Main/Transfer navigation within that session; guided feedback remains staged behind a written attempt.

## Review disposition

The six adversarial findings and their bounded repairs are detailed in `M13-RESOLUTION.md`. The prior `M13-VERIFICATION.md` is the repaired-candidate checkpoint. The user-provided close authorizes this publication; no separate written follow-up audit is archived here. CI validates implementation behavior and does not substitute for measured learner mastery. M13 clearance continues to require independent task evidence and delayed review rather than page completion.

## Verification and stop boundary

The repaired, still-unpublished implementation head `c56f88e34a4e97990825109c5a0db34390722df3` passed [full Actions run 36121162825](https://github.com/Karuma-jojo/Chronological-Deck/actions/runs/36121162825), including Chromium's staged-guidance and 34-surface probe. The first publication push exposed a compatibility regression in older modules' navigation evidence; the session-visit marker was scoped to M13 staged guidance. Published-route implementation head `b2af3c7439fd8dd6b6dae52416af186c4b8c8509` then passed [full Actions run 36128219907](https://github.com/Karuma-jojo/Chronological-Deck/actions/runs/36128219907). Syntax, structural and semantic regressions, Chromium installation and the browser evidence workflow all completed successfully. The final main head and full Actions result must be checked separately. Stop at M13: no M14 authoring, T25 edit or legacy-progress migration.
