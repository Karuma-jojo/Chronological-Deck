{
  "name": "chrono-deck-chatgpt-app",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Barebones T22 Markdown archive and synced progress ledger for Chrono-Deck.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node ../scripts/validate-t22-atomic.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.25.76"
  }
}
